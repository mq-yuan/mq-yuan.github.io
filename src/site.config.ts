// Site-level switches (not personal facts — those live in profile.yaml).
export const siteConfig = {
  /** Writing section: nav item, homepage block, footer RSS link. Hidden until
   * there are enough real posts (doc 03 §3 launch rule; author, 2026-09-11).
   * The /writing routes and rss.xml still build so nothing 404s. */
  showWriting: false,
};
